#ifndef SENSOR_VALUE_H
#define SENSOR_VALUE_H

enum SensorValue {
    NOT_SPECIFIED,

    TEMPERATURE,
    HUMIDITY,
    LIGHT_INTENSITY,
    IS_TILTED,

    DISTANCE,
    DISTANCE_FRONT,
    DISTANCE_LEFT,
    DISTANCE_RIGHT
};

#endif