export enum TopicEnums {
   RobotTopic = "1/0",
   UsSensorTopic = "1/1", //(distance in cm) int
   DhtSensor = "1/2", //(temp+hum) int
   TiltSensor = "1/3", //(is tilted or not) boolean
   PhotoResistor = "1/4" // (light intensity) int
}
